import type { Metadata } from "next";

export const metadata: Metadata = { title: "Sign in" };

// The shell renders the sign-in screen for this path (and whenever nobody is signed in).
export default function SignInPage() {
  return null;
}
