import { NewDealPage } from "@/components/escrow/pages/NewDeal";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "New deal" };

export default function Page() {
  return <NewDealPage />;
}
