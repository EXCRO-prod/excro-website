{
  "name": "excro-conditional-release",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w server",
    "test": "npm test -w server",
    "typecheck": "npm run typecheck -w server"
  }
}
